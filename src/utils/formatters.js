export function fmtTick(n)    { return n.toLocaleString(); }
export function fmtEnergy(e)  { return e.toFixed(1); }
export function fmtPct(v)     { return (v * 100).toFixed(1) + '%'; }
export function fmtCount(n)   { return String(n); }
