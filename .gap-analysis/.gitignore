# status.json is regenerated by the gap-analysis-sync-index workflow.
# Tracking it in git can cause merge conflicts; the workflow commits it on main only.
# Comment out the next line if you prefer to track it (and accept the conflict risk).
# status.json

# Local Claude Code session caches (if any tooling generates these)
.session-cache/
